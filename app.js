// write code here
โค้ดด้านล่าง มีคำสั่ง alert อันไหนบ้างที่จะทำงาน

1. if (-1 || 0) alert('first');
2. if (-1 && 0) alert('second');
3. if (null || (-1 && 1)) alert('third');

ผลลัพธ์
1 และ 3 ที่ทำงาน
2 ไม่ทำงานเพราะ เนื่องจาก if เป็น false 
